import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  progressBackground: {
    height: 6,
    backgroundColor: '#ffe0b2',
    borderRadius: 3,
    overflow: 'hidden',
  },
  progressFill: {
    height: '100%',
    backgroundColor: '#fff',
  },
});
